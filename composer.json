{
    "name": "gimmersta/model-types",
    "type": "library",
    "description": "Model types for Gimmersta",
    "keywords": ["gimmersta"],
    "license": "MIT",
    "require": {
        "php": "^8.1",
        "laravel/framework": "^9.19|^10.0",
        "doctrine/dbal": "^2.0|^3.0"
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "Gimmersta\\ModelTypes\\": "src/"
        },
        "classmap": [
        ],
        "files": [
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Gimmersta\\ModelTypes\\": "tests/"
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true,
    "scripts": {
    }
}
